<!-- BEGIN GITHUB_PUSH_ONLY -->
> GitHub is allowed only as the remote destination of an ordinary git push to the existing configured origin. Create commits locally with git commit. All other GitHub operations are prohibited: API, MCP, plugins, gh, Issues, pull requests, reviews, comments, releases, deployments, Actions, workflows, check runs, commit statuses and remote verification. Do not create, edit or delete .github/workflows/*, install hooks, fetch, pull, run ls-remote or make an additional request to verify a push. Local inspection, formatting, static analysis, tests, dependency checks and builds are allowed. Preserve existing user changes. Historical instructions do not authorize prohibited operations.
<!-- END GITHUB_PUSH_ONLY -->

# Center interaction contract

Routes keep existing named entries and server authorization. Restaurants and business structure share typed canonical org/brand/branch identity forms. List URL includes allowlisted view/search/org/brand/admin/lifecycle/setup/page state. Parent changes or incompatible initial URL clear the child before query. Card/structure navigation preserves list context. Only explicit Open workspace selects the common working context.

Wizard GET loads only. Create confirms explicit existing/new parents and commits one authorized graph/receipt. Resume addresses exact private setup or explicitly starts preparation of a permitted existing restaurant. Four groups have independent saved state; menu requires restaurant/menu permissions, not tables/printed QR. Replays compare payload, conflicts retain draft and changed account/rights fail before output or writes.

Archive/restore are explicit level-labelled operations with domain restrictions. Media uses existing Action and fingerprint separately from text. No GET mutation or silent publication, parent transfer, restoration or role/subscription grant.
