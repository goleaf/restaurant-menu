# Testing and quality gates

## Sixth audit — image retries, transport validation and bounded access (2026-09-14)

Final source manifest: `1b4a9fdd89c8794cf1dde89a7f85e23149d82b399b02f69d37ae2431bc2be416` over **1,020** code/config/manifest files, unchanged throughout final gates. Eight initial image regressions expose stale replacement/rollback-retry leaks; integrated media/configuration tests pass **97 / 452**. Two additional moved-organization fixtures initially violated the existing composite branch/brand FK; correcting their factory ownership graph produces valid scope tests without weakening constraints. Current-path removal, unrelated dirty fields and changed/archived ownership are also covered.

Actual kitchen-department transport tests initially report **16 failures / 8 passes**, then the corrected component and existing department suite pass **29 / 150**. The access/policy slice passes **54 / 545** after nine observed allocation/query-budget failures. The final combined new-test/configuration slice passes **82 / 285**. BranchUser hydration falls from 1/40/400 to zero; assigned access stays at five queries, organization-wide fallback drops six to five, and settings mount drops fourteen to thirteen for both empty/full schedules. The exact two mount budgets are tightened. Each entity image mutation intentionally adds one selected authoritative lookup; transport validation adds no queries.

Final Browser WebKit passes **5 / 433** in **29.74 seconds**. Full four-process backend passes **1,971 / 48,546** in **119.95 seconds**. Canonical sequential `composer test:coverage` passes the same **1,971 / 48,546** in **834.92 seconds**, with **93.9% application coverage**. Every run has zero failures/skips. Combined distinct backend/browser evidence: **1,976 passed tests / 48,979 assertions**, a **100% pass rate**. Clover records **26,872 / 28,616 application statements** across **520 files**; all **222 Actions** execute at **12,871 / 13,561 statements (94.91%)**. Statement coverage is separate from pass rate; OrganizationUserStatus and Settings/Appearance still have executable statements without backend execution. Three additional zero-execution files contain no coverable statements.

Pint and canonical Larastan pass with zero errors. Composer strict validation/direct inspection/audit, npm audit and production build pass, with zero dependency advisories. All **88 migrations** complete an owned SQLite migrate/reset/migrate roundtrip, followed by successful default seeding twice and isolated config/route/event/view compilation. Translation scan/audit covers **660 code files / 2,223 keys per locale / 6,669 entries**, with zero issues. Isolated Chrome login loads 12 resources with HTTP 200, no console warnings/errors and only Boost's informational notice; Boost browser logs are unavailable/empty. Telescope/Debugbar MCP is not exposed; executable query counters and read-only Boost schema inspection provide the scoped query evidence. No migration, seed or domain-data mutation ran against the application database.

The complete audit retains **49 models/factories / 88 migrations / 61 tables / 633 columns / 308 indexes / 144 foreign keys**, with no missing leading FK index or redundant nonunique prefix. Inventory remains **222 Actions / 13 controller PHP files / 4 Form Requests / 61 Livewire PHP files including 3 Forms / 18 policies / 133 Blade views**; there are **177 Unit/Feature/Browser PHP files** and **73 additional regression cases**. Existing architecture tests enforce controller/Livewire/Blade boundaries. All **174 Markdown files / 298 local link occurrences / 111 distinct file targets** pass; all **24 skill frontmatters** and full provider parity for **eight project skills** pass. The workflow skill and shared rules now capture current image references and original transport validation; other valid guidance and deployed migration history remain unchanged. Independent image review found no blocker. Final documentation checks are recorded in PROGRESS.md before publication.

## Fifth audit — required media persistence (2026-09-14)

Final source manifest: `d4a98232186afe5ed8638f07afdd1898a696c3fa5ba8fb5cb2a262a78b7b66a4` over **1,017** code/config/manifest files. Twenty-six RED cases exposed ignored saving/updating/creating/deleting vetoes and committed replacement deletion after an observer callback throws. The integrated media slice passes **83 tests / 412 assertions**, including four additional callback-writes-then-throws cases. Two subsequent stale-image cases pass **2 / 12** against the existing ownership filter; that suspicion required regression coverage, not a scope change. The preliminary parallel run passed 1,896/48,291; its coverage run was deliberately stopped after 70.94 seconds (exit 143) when the test inventory expanded. It is not a completed coverage result.

Final Browser WebKit passes **5 / 433** in **28.60 seconds**. Full four-process backend passes **1,898 / 48,303** in **129.14 seconds**; canonical sequential `composer test:coverage` passes the same **1,898 / 48,303** in **822.27 seconds**, with **93.9% application coverage**. Every run has zero failures/skips. Clover records **26,807 / 28,545 statements** across **520 application files**; all **222 Actions** execute at **12,830 / 13,520 statements (94.90%)**. Combined distinct backend/browser evidence is **1,903 passed tests / 48,736 assertions**, a **100% pass rate**. Statement coverage is separate; `OrganizationUserStatus` and `Settings/Appearance` still have no backend statement execution. The final 1,017-file source digest is unchanged after all full gates. The source includes 32 additional regression cases and no model, migration, dependency, translation or Blade changes.

Fresh read-only Boost schema/ledger inspection matches all **88 migrations** at batch 9, with **61 tables / 633 columns / 308 indexes / 144 foreign keys** and no missing leading foreign-key index. All **49 models** have factories; every migration has up/down. The owned SQLite migrate/reset/migrate roundtrip, two default seed runs and isolated config/route/event/view compilation pass. Pint and canonical Larastan pass with zero errors. Composer strict validation/direct inspection/audit, npm audit/build and translation checks pass; dependency audits report zero advisories. Translations remain **660 code files / 2,223 keys per locale / 6,669 entries**, with zero issues. The isolated Chrome login loads 12 resources with HTTP 200 and no console warnings/errors; Boost browser logs are unavailable/empty.

The architecture/factory/schema/traceability slice passes **34 / 1,337**. Inventory remains **222 Actions / 13 controller PHP files / 4 Form Requests / 61 Livewire PHP files including 3 Forms / 18 policies / 133 Blade views**; there are now **174 Unit/Feature/Browser PHP files**. Controller scans find no direct validation or model persistence; existing architecture tests enforce Livewire/Blade boundaries. All **173 Markdown files / 295 local targets** pass link checks, and all **eight project skills** retain exact provider parity. The Eloquent reference now documents false-return semantics and rollback compensation in each provider; a shared Boost rule records the concrete media contract. Valid existing definitions and migration history remain unchanged.

## Fourth audit — bounded media cleanup and strict factory graphs (2026-09-14)

Current source manifest: `fbb58bf382b6cbe61e1866d69182dc23782e93e3872a45acad978cfc3f53438b` over **1,016** code/config/manifest files. RED reproductions covered parent media retention, malformed foreign-menu links, cyclic discovery, duplicate fallback deletion events, root/child/item event vetoes, strict partial factory graphs and department-readiness order totals. The existing fallback hydration regression caught an extra freshly hydrated model during implementation; a scoped active `exists()` recheck preserves its 200-model limit without weakening the test.

Focused media/transaction/schema verification passes **51 tests / 361 assertions**, including 605-item media traversal, 405-child query filters, temporary-file write failures/short writes and transaction lifecycle failures. Factory/schema/dispatch verification passes **67 / 616**. Full Browser WebKit passes **5 / 433**, zero failures/skips, in **31.53 seconds**. The full four-process `php artisan test --compact --parallel --processes=4 --testsuite=Unit,Feature` passes **1,866 / 48,164**, zero failures/skips, in **143.14 seconds**. Canonical sequential `composer test:coverage` passes **1,866 / 48,166**, zero failures/skips, in **826.71 seconds**, with **93.9% application coverage**. Clover records **26,795 / 28,535** covered statements across **520** application files; all **222 Actions** execute, covering **12,818 / 13,510** statements (**94.88%**), with no zero-execution Action. Two added traceability path checks account for the two additional sequential assertions. The 1,016-file code manifest is unchanged after browser, parallel and coverage runs. Combined distinct backend/browser evidence: **1,871 passed tests / 48,599 assertions**, a **100% pass rate**; statement coverage is separate.

Pint and canonical Larastan pass with zero errors. Composer strict validation/direct dependency inspection/audit, npm audit and production build pass; both dependency audits report zero advisories. All **88** migrations complete an owned SQLite-file migrate/reset/migrate roundtrip, followed by default seeding twice. Runtime database, storage, compiled views and config/route/event caches use owned temporary paths. Translation scan/audit covers **660** code files, **2,223** keys per locale and **6,669** entries with zero issues. A fresh isolated Chrome context loads the Herd login and its eleven assets with HTTP 200 and no console warnings/errors; the only console entry is Boost's informational logger notice, and Boost browser logs have no available entries. No schema migration, seed or domain-data mutation ran against the application database.

The audit covers **49 models/factories**, **88 migrations**, **13 controllers**, **four Form Requests**, **61 Livewire PHP files** including **three Form objects**, and **222 Actions**. Existing architecture tests and a fresh source scan find no direct controller validation/model writes, Livewire queries/model writes, or first-party raw SQL. All **173 Markdown files / 295 local targets** pass the link audit; all **eight** project skills retain exact provider parity. Two Laravel reference files needed traversal corrections in all three copies; shared rules now capture media lifetime and factory graph invariants. Independent final media review found no remaining blocker. No schema/dependency rewrite or deployment was required; retained-memory and query-cost limits are in `performance.md` and `known-limitations.md`.

## Third audit — menu media, weekly schedules and factory states (2026-09-14)

Current source manifest: `63f0548b55ebcbca60930210acbb1c90f21929e73f423b292bb27f91b633fbf2` over 1,013 code/config/manifest files (including two existing `.gitignore` files not included in the prior manifest). Earlier results below belong to their stated source snapshots.

RED evidence: six menu-media lifecycle failures; six initial schedule overlap/next-opening failures; a malformed nested interval; an invisible fifth interval; seven factory-state failures plus cancelled-line total accounting; five DST occurrence failures and one normalized-start ordering failure. Corrections preserve transaction callbacks, current tenant scope, bounded form state, recurring overnight semantics and integer money. Initial DST expectations were corrected for the configured locale before counting the five application failures.

Fresh focused evidence includes menu media/gallery/CRUD/schema **65 / 521**, factory/schema/dispatch **45 / 575**, and DST/opening/closure **33 / 159**. Browser WebKit passes **5 / 433**, zero failures/skips, in **32.79 seconds**; it now exercises overlap rejection, unchanged persisted profile data and successful correction/retry over real Livewire transport. Full four-process `php artisan test --compact --parallel --processes=4 --testsuite=Unit,Feature` passes **1,831 / 47,956**, zero failures/skips, in **130.72 seconds**. Canonical sequential `composer test:coverage` passes the same **1,831 / 47,956**, zero failures/skips, in **821.14 seconds**, with **93.9% application coverage**. Clover records **26,747 / 28,481** covered application statements across **519** files. All **221 Actions** execute, with **12,793 / 13,481** statements covered (**94.90%**), and no Action has zero execution. The source digest is unchanged after browser, parallel and coverage runs. Combined distinct backend/browser result: **1,836 tests / 48,389 assertions**, a **100% test pass rate**; code coverage is reported separately.

Pint, Larastan, Composer validation/audit, npm audit/build, translation scan/audit and isolated config/route/event/view compilation pass. All 88 migrations complete an owned SQLite-file migrate/reset/migrate roundtrip, followed by successful default seeding twice; existing seeder/idempotency tests are part of the full suite. No schema migration, seeding or domain-data mutation was run against the application database in this audit. Read-only live schema inspection agrees with the complete migration schema (61 tables, 633 columns, 308 indexes, 144 FKs). A fresh isolated Chrome context loads the Herd login plus its 11 assets with HTTP 200 and no console warnings/errors; Boost browser logs are unavailable or empty.

All **172** first-party Markdown files and **295** local link targets pass the link audit. All **eight** project skills were reviewed; three Laravel reference files needed semantic corrections, synchronized across `.agents`, `.claude` and `.cursor`. The other seven skills remain correct. Shared rules record recurring schedule and menu-file transaction constraints. Independent final reviews found no blocker in the changed media, factory and schedule scope. No dependency update, historical migration rewrite or production deployment was required.

## Second audit — atomic branch configuration (2026-09-14)

Current source: `d25a0ff6daf9547527f872a349d1b95ee62cedd669c6775ba36f78e01f4399fc` over 1,006 code/config/manifest files. The earlier repository-wide result below belongs to `930059f` and its preceding implementation.

RED evidence: two late-save cases persisted earlier settings/profile changes before the fix; generic media lifecycle tests exposed premature deletion and rollback leaks; duplicate weekday submission was accepted; the browser showed a disabled percentage field after its switch was enabled. The first browser attempt incorrectly filled a disabled input and was terminated without a result; the corrected interaction then produced the meaningful RED assertion before `.live` was added. A mistyped parallel wrapper invocation was rejected before tests started, then corrected to `--testsuite=Unit,Feature`.

The final focused aggregate/Form/media/traceability batch passes **42 tests / 1,248 assertions**. Separate direct Action/query coverage passes **9 / 45**, including current membership/tenant rechecks and a constant **14-query** mount for 0/28 intervals versus **17** on the baseline fixture. Final Browser WebKit passes **5 / 424**, zero failures/skips, in **26.17 seconds**; the expanded administration journey saves and reloads the new Form through real Livewire transport and retains responsive/accessibility/console checks. Full `PAO_DISABLE=1 php artisan test --compact --parallel --processes=4 --testsuite=Unit,Feature` passes **1,771 / 47,713**, zero failures/skips, in **129.83 seconds**. Canonical sequential `PAO_DISABLE=1 composer test:coverage -- --coverage-clover=<owned-temporary-path>` exits 0 with the same **1,771 passed / 47,713 assertions**, zero failures/skips, in **799.62 seconds**, at **93.8% application coverage**. Clover records 26,665/28,404 covered application statements across 518 files. All **221 Actions** execute: 12,754/13,445 statements (**94.86%**) and zero Actions without execution. The source digest is unchanged after all three full runs. Combined distinct backend/browser result: **1,776 passed / 48,137 assertions**, a **100% test pass rate**, with code coverage reported separately.

Pint, Larastan, Composer validation/audit, npm audit/build, translation scan/audit and owned config/route/event/view compilation pass. Translation scan covers 658 files with 2,222 aligned keys per locale and no issues. A fresh isolated Chrome context on the Boost-resolved Herd login loads all 12 document/assets with HTTP 200 and no console warnings/errors; Boost browser logs contain no available entries. Local schema reconciliation is in `MIGRATION_AUDIT.md`: 88/88 applied after a checked backup, unchanged rows, integrity OK and zero FK violations.

All 172 tracked/untracked first-party Markdown files were checked for links: 295 local targets, zero unresolved. All eight project skills were reviewed; the workflow skill required compound-save/media/local-migration guidance and its three provider copies are identical. Independent implementation, authorization and pressure reviews found no remaining blocker after the local-backup guidance was made explicit. No dependency, historical migration, valid model or factory rewrite was needed.

## Repository-wide audit — 2026-09-14

The current source includes report-generation fences, separate-cache commit/rollback handling, bounded expired-report cleanup, factory query reuse, server-side optional-auth guards and persistent password confirmation for Livewire JSON/browser requests. All eight formerly feature-skipped auth cases now explicitly enable their feature inside an isolated test application. Runtime Fortify defaults remain disabled; no assertion is skipped or weakened to report success.

Observed focused evidence: cache/factory integration **73 tests / 1,010 assertions**; auth/settings/routes/architecture **121 / 414**, plus backup routes **17 / 145**. The final WebKit Browser run passes **5 / 415** in **26.20 seconds**. Model/schema audit before the cache mapping covered all 48 domain models/factories and 88 migrations with **107 / 1,319**; the new cache model/factory is also covered by the current factory audit. All 88 migrations completed an isolated SQLite `:memory:` migrate/reset/migrate roundtrip. Populated-data rollback restrictions are in `MIGRATION_AUDIT.md`.

Regression evidence includes eight cold/deferred/lost-registry/expired-lease cases that fail against the original report Actions from `0ce85ba`, two separate-connection cases that fail without transaction-end invalidation, a real two-process initialization barrier, cleanup renewal/failure/batch-limit cases, 18→2 factory source reads, and real HTTP Livewire requests that previously mutated credentials after password confirmation expired. Initial test-fixture mistakes were corrected before the final baseline comparison; they are not counted as evidence of an application defect.

Pint, Larastan, strict Composer validation and zero-advisory Composer/npm audits pass. Clean disposable Composer/npm locked installs succeeded with lifecycle scripts disabled; workspace `npm ci --ignore-scripts` and Vite 8.2.2 production build pass. Isolated config/route/event/view compilation exits 0 for all four commands, without clearing shared caches. Translation scan/audit reports 656 files, 2,222 aligned keys per locale, 6,666 entries and zero issues.

The final four-process Unit/Feature run passes **1,738 tests / 47,528 assertions**, **zero failures and zero skips**, in **112.82 seconds**. Browser and parallel runs use unchanged SHA-256 `263cee00e10db1d1f3d96363ffa89b6b9b499732da45b6b1f2acc69becc2aa57` over 1,002 code/config/manifest files. The canonical sequential `PAO_DISABLE=1 composer test:coverage -- --coverage-clover=<owned-temporary-path>` run exits **0** with the same **1,738 passed / 47,528 assertions**, **zero failures or skips**, in **789.68 seconds**, and **93.8% application coverage**. Clover records 26,593/28,330 covered application statements. All **220 Actions** execute, with **12,714/13,404 statements covered (94.85%)** and zero Actions without execution. The source digest remains identical after all three final runs. The combined distinct backend/browser result is **1,743 passed tests / 47,943 assertions**: a 100% pass rate, not 100% code coverage. Older results below remain historical.

The first full parallel attempt found three cancellation-budget expectations and one process-fixture isolation mismatch. Cancellation intentionally adds two generation deletions; the fixture now requires exactly 21 queries for 0, 3 and 501 fulfilled items while retaining exact audit counts and zero item hydration. Laravel's parallel test lifecycle adds a cache prefix to the parent; process children now receive that actual prefix so the concurrency assertion compares the same store. The focused process regression changed from RED to **10 / 33 PASS** before the successful full rerun. No production assertion was weakened to mask a failure.

All eight project-local skills were audited; seven entrypoints and 17 referenced Markdown files required corrections. Canonical `.agents` skills were synchronized into the tracked `.claude` and `.cursor` copies after confirming no provider-specific differences. Frontmatter, local links and applicable PHP/JS examples pass; the final tracked Markdown audit checked 169 files and 289 local link paths with zero unresolved targets; an isolated assertion-reference check passes 3 tests / 6 assertions. Workflow pressure review confirms that shared cache clearing and reuse of stale PASS evidence are forbidden.

Pest 4 is the sole primary PHP test style. Feature tests cover Laravel/Livewire integration and security boundaries; unit tests cover pure rules and translation contracts. Browser automation is reserved for DOM, focus, responsive, navigation and console behavior that PHP tests cannot prove.

## Development loop

1. Add or update a failing focused test.
2. Run `php artisan test --compact <path-or-filter>`.
3. Implement the smallest correct change and rerun the target.
4. Run `vendor/bin/pint --dirty --format agent` after PHP edits.
5. Run `vendor/bin/phpstan analyse --memory-limit=1G` on the stabilized pass.
6. Run `npm run build` after Blade/CSS/JavaScript source changes.
7. Update the requirement matrix only after observed evidence.

Tests use isolated SQLite, fake local disks and faked external I/O. Never run `migrate:fresh` against the application database. Factories create valid records; no test or seeder requires public internet or real credentials.

If the shared checkout contains local compiled configuration, point `APP_CONFIG_CACHE`, `APP_ROUTES_CACHE`, `APP_EVENTS_CACHE` and `VIEW_COMPILED_PATH` to owned disposable paths before verification. Confirm the effective testing database is isolated. Do not clear shared application caches merely to prepare a test run.

`composer test:coverage` is the canonical coverage command. It runs the Unit and Feature suites against `app/` and fails below 90%. The current checkout has no `.github` workflow directory; this audit supplies local command evidence, not a GitHub Actions run. The local Herd PHP 8.5 CLI loads Xdebug with `xdebug.mode=off`; the Composer script enables coverage only for this command through `XDEBUG_MODE=coverage`, so normal local and production requests do not pay coverage overhead.

## Export and report-cache follow-up (2026-09-14)

The bounded plan is in `IMPLEMENTATION_PLAN.md`. New HTTP export tests parse complete CSV rows with `fgetcsv(..., escape: '')`: 19 payment-cell variants cover formula prefixes, whitespace/control characters, full-width forms, legitimate Unicode, exact money, quoting, backslashes and newlines; four cases cover every export type. Two dashboard cases warm the cache in opposite ViewOrders/ConfirmOrders user orders, assert distinct keys and correct warm links, and verify authorized/forbidden waiter HTTP access. Two report-cache strategies each exercise 51 variants, displaced metadata/payload removal, pending refresh cancellation, retained reuse and subsequent branch invalidation.

RED was observed before implementation: the combined new CSV/dashboard cases produced 20 failures / 5 passes / 153 assertions; both overflow cases failed because the displaced payload remained cached (2 failures / 8 assertions). After implementation, `DataExportsTest`, `RestaurantDashboardTest`, `BasicAnalyticsTest` and `BranchCacheInvalidationTest` pass **63 tests / 853 assertions** in 6.96 seconds. `vendor/bin/pint --dirty --format agent` passes; `composer analyse` reports zero errors. `PAO_DISABLE=1 php vendor/bin/pest tests/Browser --compact --browser safari` passes **5 scenarios / 415 assertions** in 29.99 seconds with Playwright WebKit. Translation scan reports 652 files, 2,222 used/JSON keys and zero missing/unused/phrase keys; the three-file, 6,666-entry audit has zero critical issues.

The first canonical coverage attempt failed with 1 failed / 1,677 passed / 8 skipped / 47,096 assertions in 785.43 seconds. Its one failure was a Faker-generated duplicate branch name in the new 52-branch overflow fixture (`branches.brand_id, branches.name`), not a cache assertion. The fixture now assigns deterministic sequence names. Its isolated rerun passes 2 / 318; the final five-file regression batch, including traceability, passes **64 / 1,859** in 6.72 seconds. The traceability regression initially rejected the accurate partially-implemented release status; it now accepts that documented status and additionally checks all 51 row statuses against the compliance matrix. Required evidence fields and implementation paths remain enforced.

The final four-process Unit/Feature run passes **1,678 tests / 47,307 assertions**, with eight feature-gated skips, in 224.74 seconds. The final canonical `PAO_DISABLE=1 composer test:coverage -- --stop-on-failure` run exits 0: **1,678 passed / 8 skipped / 47,307 assertions**, **93.5% application coverage**, in 816.82 seconds. Repeated Pint and Larastan checks pass. The earlier failed attempt used SHA-256 `34a708ea4ff04cafd07f7c983938650b6d6383935f407b3f3b2b403e7a2e58f0`; the final source digest is `c9edb6d5dd9355f14aaae856a31ee1255da056241dc37775003890e6781d8dd5` across the same 988-file set described below. Application code did not change after the successful browser suite; only the deterministic fixture and documentation-consistency test changed before this final verification.

The new CSV boundary adds no persistence queries. Waiter-link preparation reuses the resolved ViewOrders branch set; no separate before/after SQL or latency benchmark was taken for that removal. Registry overflow intentionally adds two cache deletions per displaced key; normal operation at or below 50 variants does not add eviction writes. Existing query-budget tests remain the executable query evidence. Overflow/deferred tests do not prove invalidation during an already-running refresh or concurrent registry updates. Native spreadsheet save/reopen behavior, production timing and deployment were not tested. The independent review attempt was blocked by the agent service usage limit; local review of the changed Actions, tests and documentation is complete.

## Complete article review (2026-09-14)

All 44 supplied articles were retrieved again and compared with installed Laravel 13.26.1, PHP 8.5 and the current application. The table records applicability, not a requirement to introduce every helper. Existing domain, SQLite, authorization and Blade contracts take precedence over incompatible examples. The latest pass adds report-cache stale-while-revalidate behavior and removes duplicate dispatch relationship reads; earlier changes remain in the shared worktree.

The new dispatch regression initially failed with 3/11 ticket-item SELECTs for 2/10 departments, then passed with one in both cases. The complete dispatch file passes 7 tests / 93 assertions. Twelve new cache cases initially had 10 failures (no deferred refresh) and two passing expiration cases; the analytics/dashboard/branch-invalidation files now pass 29 tests / 244 assertions. Pint, Larastan, Composer validation/audit, translation audit/scan and the production Vite build pass. JavaScript dependencies were restored from the unchanged package manifest with install scripts disabled and no root lockfile creation. npm audit reports zero advisories for the installed dependency tree using an isolated temporary copy of its hidden lock metadata. The final canonical `PAO_DISABLE=1 composer test:coverage` run exits successfully: 1,651 Unit/Feature tests passed, 8 skipped, 46,755 assertions in 756.64 seconds; application coverage is 93.6%, above the 90% gate. This includes the earlier article changes and the new cache/dispatch regressions. The initial coverage attempt was interrupted to finish a cache repository type correction; only this final run supplies completion evidence. Browser, parallel and deployment/runtime-cache checks were not rerun during that initial backend pass (the follow-up browser/parallel results are recorded below); no template, route, schema or dependency declaration changed, and no application database was migrated or seeded.

The source inventory stayed unchanged during the final coverage run: SHA-256 `4a8afeb32a37d7c282a9897158c1d2740cfb636ce69f354d04ba4a8d123796be` across 988 PHP/JSON/JS/CSS files in app, database factories/migrations/seeders, tests, routes, config, resources and lang, plus composer.json, package.json and phpunit.xml. The follow-up browser verification confirmed the same digest; only documentation was updated afterward.

Follow-up browser verification on the same source passes all five scenarios / 415 assertions in 35.46 seconds with `PAO_DISABLE=1 php vendor/bin/pest tests/Browser --compact --browser safari` (Playwright WebKit 26.5, not the installed Safari application). The suite covers organization administration, onboarding and the order-to-paid-table-closure journey, allergen labels, kitchen timers and waiter notification preferences, including its existing locale, responsive, focus, 200% root-text scaling and JavaScript assertions. The first execution failed before any assertions because the matching browser binary was absent; installing WebKit revision 2336 and FFmpeg revision 1011 for the already-installed Playwright 1.62.1 resolved that setup failure without changing dependency declarations. Pest 4.3.1's browser option accepts `--browser safari`; its diagnostic example mentioning `webkit` is misleading. See the [Pest browser selection documentation](https://pestphp.com/docs/browser-testing#using-other-browsers) and [Playwright browser installation documentation](https://playwright.dev/docs/browsers).

Separate Chrome DevTools and Playwright MCP smoke checks used disposable isolated Chrome profiles with the sandbox enabled and the HTTPS site resolved through `herd sites` (Boost URL tooling was unavailable). The login page returned 200 for all 12 observed resources in each tool, with no warning/error console messages. DevTools verified 320px dark and 1,440px light layouts without horizontal overflow, labelled fields and a visible keyboard skip-link focus ring; Playwright verified 320px/390px reflow and the unauthenticated onboarding redirect to login. These smoke checks performed no application-domain mutations. Physical devices, assistive technology and native browser zoom were not tested.

The subsequent `PAO_DISABLE=1 php vendor/bin/pest --testsuite=Unit,Feature --compact --parallel --processes=4` run passes 1,651 tests / 46,755 assertions with eight skips in 106.58 seconds, exit 0. Browser and backend runs were sequential stages, with isolated SQLite test databases and fake local storage. Use the named suites for this parallel command: ParaTest accepts one positional path, so passing both directory paths fails argument parsing before tests execute. This follow-up closes the browser and parallel verification gaps from the initial backend pass; deployment and runtime-cache compilation were not repeated. No additional application/test source edits, dependency declaration changes, database migrations or seeds were needed.

### Article-by-article decisions

| # | Article | Decision | Application evidence / reason |
|---|---|---|---|
| 1 | [match](https://www.vivekmistry.in/lara-blogs/replace-long-if-else-chains-in-laravel-using-php-match-expressions/) | Present | Enums and AuditLogValueSanitizer use strict match expressions; ordered authorization guards remain explicit. |
| 2 | [whenLoaded](https://www.vivekmistry.in/lara-blogs/return-only-the-data-your-laravel-api-needs-with-whenloaded/) | No API Resource boundary | The application returns prepared Blade/Livewire data, not first-party JsonResource responses. Adding an API only to use this helper would change the product. |
| 3 | [Form objects](https://www.vivekmistry.in/lara-blogs/organize-large-forms-with-laravel-form-objects/) | Present | RestaurantSetupForm, InvitationForm and BranchSettingsForm own substantial validation; focused Actions already own the multi-model workflows illustrated by the article. |
| 4 | [Custom validation rules](https://www.vivekmistry.in/lara-blogs/create-reusable-custom-validation-rules-in-laravel/) | Applied | DecimalMoney centralizes decimal parsing and overflow rejection while preserving field rules and EN/LT/RU failures. |
| 5 | [whereAll](https://www.vivekmistry.in/lara-blogs/replace-multiple-where-conditions-with-whereall-in-laravel/) | No useful rewrite | Current multi-column searches match any field; tenant/status predicates use different values, while invitation null-scope guards remain explicit whereNull calls. Requiring one keyword in every field would change search results. |
| 6 | [mapToGroups](https://www.vivekmistry.in/lara-blogs/stop-writing-nested-loops-in-laravel-collections-using-maptogroups/) | Applied | StaffQueryService and BuildWaiterDashboardAction transform zone assignments into grouped identifiers without intermediate grouped model collections. |
| 7 | [before / after](https://www.vivekmistry.in/lara-blogs/navigate-collections-easily-in-laravel-using-before-and-after/) | No equivalent workflow | Order and session transitions form branching state machines; onboarding includes guarded recovery. Neighbor lookup cannot replace those transition contracts. |
| 8 | [whereAny](https://www.vivekmistry.in/lara-blogs/simplify-complex-conditions-in-laravel-using-whereany/) | Applied | Branch, service-point and staff searches share grouped OR predicates and retain tenant constraints and the literal search value 0. |
| 9 | [sole](https://www.vivekmistry.in/lara-blogs/need-exactly-one-item-use-sole-on-laravel-collections/) | No equivalent cardinality rule | Optional and deliberately ordered first-match lookups remain optional. Unique database constraints protect identities; sole would introduce exceptions for legitimate absence. |
| 10 | [whereBelongsTo](https://www.vivekmistry.in/lara-blogs/stop-writing-manual-foreign-key-queries-using-wherebelongsto/) | Applied | Sixteen staff/waiter relationship predicates use explicitly named model relationships; additional organization and brand constraints remain. |
| 11 | [Constrained eager loading](https://www.vivekmistry.in/lara-blogs/your-laravel-with-queries-may-be-loading-too-much-data/) | Applied and extended | Waiter draft totals use withSum; menu translation scalars avoid extra model hydration; dispatch no longer loads each ticket item collection twice. Nested relations retain both primary and foreign keys. |
| 12 | [Cache::flexible](https://www.vivekmistry.in/lara-blogs/serve-cached-data-instantly-in-laravel-using-cacheflexible/) | Applied in this review | Dashboard uses [45, 60], analytics [240, 300], with original-locale refresh, database locks and cancellation through invalidation of both value and creation timestamp. |
| 13 | [lazyById](https://www.vivekmistry.in/lara-blogs/reduce-memory-usage-in-laravel-using-lazybyid/) | Applied | Menu/category media reads and cascades use 200-record ID batches; file paths spool until commit, and individual model events still run. Category visited IDs remain proportional to the tree. Brand/organization cascades remain bounded. |
| 14 | [makeHidden](https://www.vivekmistry.in/lara-blogs/conditionally-hide-model-attributes-in-laravel-using-makehidden/) | Permanent hiding applied | Guest/session-join credential fields are model-hidden for every serialization. Prepared public payloads remain explicit allowlists; no conditional exposure is required. |
| 15 | [exists instead of count](https://www.vivekmistry.in/lara-blogs/avoid-heavy-queries-in-laravel-using-exists-instead-of-count/) | Applied | Boolean existence checks use exists/doesntExist. Owner-role mutation avoids an unnecessary existence query for already inactive membership; exact business counts remain counts. |
| 16 | [isDirty / wasChanged](https://www.vivekmistry.in/lara-blogs/prevent-accidental-attribute-changes-in-laravel-using-isdirty-waschanged/) | Applied | MenuItemObserver skips audit context reads unless price or availability changed. Existing model invariants use dirty-state checks at the appropriate lifecycle stage. |
| 17 | [reorder](https://www.vivekmistry.in/lara-blogs/simplify-complex-sorting-in-laravel-using-reorder/) | Present and used | Batch cascades remove display ordering before advancing by ID, preserving safe mutation traversal. |
| 18 | [loadCount](https://www.vivekmistry.in/lara-blogs/load-relationship-counts-efficiently-in-laravel-using-loadcount/) | Applied | Order cancellation records full related-item totals without loading a capped 500-item collection; the 501 matching-item regression preserves exact history. |
| 19 | [clone](https://www.vivekmistry.in/lara-blogs/reuse-complex-query-logic-cleanly-in-laravel-using-clone/) | Applied | PublicQrQueryService reuses isolated clones of a scoped base query; current, merged and authorized transferred-session restoration remain distinct. |
| 20 | [Http::pool](https://www.vivekmistry.in/lara-blogs/reduce-duplicate-api-calls-in-laravel-using-httppool/) | No outbound HTTP workload | There are no first-party Http calls to parallelize. Pooling would overlap independent requests, not deduplicate repeated requests. |
| 21 | [addSelect](https://www.vivekmistry.in/lara-blogs/generate-dynamic-query-columns-in-laravel-using-addselect/) | Applied | Guest menu category/item translations are correlated scalar selects with owner/locale keys and limit(1); no raw SQL is introduced. |
| 22 | [withWhereHas](https://www.vivekmistry.in/lara-blogs/clean-relationship-filtering-in-laravel-using-withwherehas/) | Applied | Department dashboards reuse the same constrained item predicate for ticket selection and eager loading; all active/history filters retain their result sets. |
| 23 | [app()->call](https://www.vivekmistry.in/lara-blogs/auto-resolve-dependencies-anywhere-in-laravel-using-app-call/) | No dynamic invocation boundary | Controllers and Actions already use container-managed dependency injection. No configurable callback dispatch requires app()->call. |
| 24 | [withoutEvents](https://www.vivekmistry.in/lara-blogs/skip-model-events-in-laravel-using-withoutevents/) | Unsafe suppression removed | DatabaseSeeder no longer suppresses model events required for QR/session/call guard keys. Bulk Eloquent updates already bypass per-model events; wrapping them adds no benefit. |
| 25 | [Conditionable](https://www.vivekmistry.in/lara-blogs/run-code-only-when-conditions-match-using-conditionable-trait-in-laravel/) | Present through builders | Existing Eloquent/Collection when chains already supply conditional composition. Stateless one-operation Actions do not need a second fluent service API. |
| 26 | [preventLazyLoading](https://www.vivekmistry.in/lara-blogs/avoid-silent-failures-in-laravel-using-preventlazyloading/) | Applied | AppServiceProvider enables it outside production; waiter inactivity checks reuse authorized preloaded branches and settings. Strict-guard and query-budget tests cover the behavior. |
| 27 | [fragment / include / components](https://www.vivekmistry.in/lara-blogs/at-fragment-vs-at-include-vs-blade-components-which-one-should-you-use/) | Existing components retained | Reusable Blade/Flux components already own presentation. The fragment example confuses partial response extraction with cached rendering/reuse; no partial-response endpoint needs fragments. |
| 28 | [mergeIfMissing](https://www.vivekmistry.in/lara-blogs/set-default-request-values-in-laravel-using-mergeifmissing/) | No missing-default duplication | Form Requests and Livewire Forms already own input preparation/defaults. Adding request values globally would change omitted/null/filter semantics. |
| 29 | [whenFilled](https://www.vivekmistry.in/lara-blogs/avoid-multiple-condition-checks-in-laravel-using-whenfilled/) | No equivalent HTTP filter path | Read Actions receive validated typed filters; existing when predicates preserve empty, false and 0 distinctions. Request helpers are not applied to unrelated Livewire state. |
| 30 | [loadMissing](https://www.vivekmistry.in/lara-blogs/avoid-n1-query-problems-in-laravel-using-loadmissing/) | Applied | GuestEntryQueryService completes a partially loaded table/area graph without reloading its parent. Variant factory states also complete missing item/menu/branch relations under strict loading; fully eager factory graphs use zero extra reads. |
| 31 | [Temporary signed URLs](https://www.vivekmistry.in/lara-blogs/generate-temporary-signed-urls-in-laravel-for-secure-downloads/) | Existing stronger download boundary retained | Reports and backups require authenticated policy checks; sensitive backup operations also require recent confirmation and replay protection. No anonymous expiring-share requirement exists. |
| 32 | [chunkById](https://www.vivekmistry.in/lara-blogs/stop-memory-issues-in-laravel-using-chunkbyid/) | Present | The four growing CSV export streams use bounded chunkById batches with the relationships needed by each export. |
| 33 | [Advanced when](https://www.vivekmistry.in/lara-blogs/advanced-when-patterns-in-laravel-you-probably-dont-use/) | Present | Conditional Eloquent queries already use when where helpful. Three-state filters and prioritized business guards keep explicit branches. |
| 34 | [Laravel Scout](https://www.vivekmistry.in/lara-blogs/supercharge-search-in-laravel-using-laravel-scout/) | Incompatible setup example | Scout is not installed. Its database search engine does not support the SQLite-only baseline; the collection engine loads records into memory. Current scoped/paginated search remains Eloquent. |
| 35 | [Benchmark](https://www.vivekmistry.in/lara-blogs/quickly-measure-performance-in-laravel-using-benchmark/) | Used for verification | The timezone helper was measured with Benchmark::measure; query/hydration regression tests provide reproducible application evidence. Benchmark::dd is not inserted into production responses. |
| 36 | [Number::clamp](https://www.vivekmistry.in/lara-blogs/control-numeric-limits-in-laravel-using-numberclamp/) | Applied | Audit/department pagination, polling intervals and alert cooldowns use numeric bounds. Invalid order quantities continue to fail validation instead of silently changing the order. |
| 37 | [data_set](https://www.vivekmistry.in/lara-blogs/modify-nested-data-easily-in-laravel-using-data-set/) | No equivalent nested write | Current append, reindex and aggregate operations do not mean assigning one nested value. data_set would not persist model changes or replace validated domain Actions. |
| 38 | [data_get](https://www.vivekmistry.in/lara-blogs/simplify-complex-array-access-in-laravel-with-data-get/) | Present; article correction retained | Existing nested reads use data_get. A wildcard missing total returns null in the shown array example, not the supplied outer default 0; null/absence semantics are preserved. |
| 39 | [defer](https://www.vivekmistry.in/lara-blogs/speed-up-laravel-requests-with-defer-run-code-only-when-its-actually-needed/) | Used correctly through flexible | Report-cache refresh runs after response termination. The article incorrectly treats defer as an on-demand value; critical writes, audit and invalidation remain synchronous. |
| 40 | [once](https://www.vivekmistry.in/lara-blogs/stop-repeating-expensive-logic-in-laravel-use-once-the-right-way/) | Applied | RestaurantSetupOptions memoizes the timezone catalogue per request; mutable authorization, language-dependent option lists and model reads are not memoized. |
| 41 | [Policies / Gates](https://www.vivekmistry.in/lara-blogs/policies-vs-gates-in-laravel-the-practical-difference-developers-actually-need/) | Present | Eighteen policies own resource permissions; broad access uses centralized capabilities. Gate calls dispatch policies, including create/viewAny methods without an instance. |
| 42 | [cursor / lazy](https://www.vivekmistry.in/lara-blogs/cursor-vs-lazy-in-laravel-choose-the-right-one-for-large-datasets/) | Safe batching retained | Exports need eager loading and keep chunkById; mutation cascades use lazyById. lazy does not release connections between chunks, and changing its filtered columns can skip rows. |
| 43 | [value](https://www.vivekmistry.in/lara-blogs/use-value-for-lazy-execution-run-heavy-code-only-when-needed/) | Incorrect fallback claim rejected | PHP ?? already evaluates its right side only when needed; wrapping the same fallback in value adds no laziness. Existing native short-circuit expressions remain. |
| 44 | [Str::of](https://www.vivekmistry.in/lara-blogs/clean-up-messy-string-logic-with-strof-fluent-strings-in-laravel/) | Applied | Upload and backup path normalization uses fluent replace/explicit slash trimming and toString; byte-based path prefix removal and containment checks remain intact. |

API semantics were checked against installed framework implementations and primary documentation: [conditional resource relationships](https://laravel.com/docs/13.x/eloquent-resources#conditional-relationships), [eager loading](https://laravel.com/docs/13.x/eloquent-relationships#eager-loading), [cache refresh](https://laravel.com/docs/13.x/cache#swr), [Blade fragments](https://laravel.com/docs/13.x/blade#rendering-blade-fragments), [deferred functions](https://laravel.com/docs/13.x/helpers#deferred-functions), [Scout database engine](https://laravel.com/docs/13.x/scout#database-engine), and [PHP null coalescing](https://www.php.net/manual/en/language.operators.comparison.php#language.operators.comparison.coalesce). Boost/schema/Telescope MCP was unavailable; source, migrations and executable test query counters supplied local evidence.

## Fluent media path normalization (2026-09-14)

`StoreLocalImageAction` and `CreateMediaZipBackupAction` use `Str::of` chains for path separator normalization and explicit slash-only trimming, with `toString()` at string return boundaries. Backup paths retain native byte-based prefix removal and directory containment checks. The existing `LocalMediaStorageTest`, `MediaBackupTest` and `MenuItemImageGalleryTest` pass 33 tests / 207 assertions in 3.19 seconds, including unsafe upload paths, archive contents, excluded external symlinks and media lifecycle behavior. Pint, `composer analyse` and diff whitespace checks pass.

This behavior-preserving refactor changes no schema, dependencies, translations or templates and makes no query or performance improvement claim. Tests use isolated SQLite and fake local disks; the application database was not changed. Full-suite, coverage, parallel, build and browser checks were not rerun for this change. Fluent string semantics were checked against installed Laravel 13.26.1 and the [official documentation](https://laravel.com/docs/13.x/strings#fluent-strings).

## Request-local timezone catalogue (2026-09-14)

`RestaurantSetupOptions::timezoneOptions` now memoizes its prepared labels with `once` for reuse by onboarding validation and rendering during a request. The existing `OnboardingRestaurantWizardTest` and `InputValidationRulesTest` pass 231 tests / 2,496 assertions in 67.85 seconds, including finite option catalogues, form validation, localization and repeated onboarding workflows. Pint, `composer analyse` and diff whitespace checks pass. The repeated-call CLI measurement and its limits are recorded in `performance.md`.

This focused optimization adds no schema, dependency, translation or template changes. Tests use isolated SQLite and fake external I/O; the application database was not changed. The full suite, coverage, parallel, build and browser checks were not rerun for this change.

## Operational numeric bounds (2026-09-14)

`Number::clamp` replaces nested `min`/`max` expressions for audit pagination (10–100), department pagination (1–50), branch polling (1–60 seconds) and production-error alert cooldowns (60–86,400 seconds). Integer types and existing bounds are preserved. `GuestActions` reuses `GetBranchPollingIntervalAction::normalize`, as the other guest polling components already do. `OrderItemQuantity` continues rejecting values outside 1–99; clamping is not a substitute for input validation. See the [official numeric helper documentation](https://laravel.com/docs/13.x/helpers#method-number-clamp).

The existing branch-settings, branch-cache, guest-table-shell, guest-invite, audit-log, kitchen-screen, production-observability and order-quantity tests pass 68 tests / 466 assertions. After formatting, the complete sequential `php artisan test --compact tests/Unit tests/Feature` run passes 1,637 tests with 8 feature-gated skips / 46,619 assertions in 224.40 seconds. Pint, `composer analyse` and diff whitespace checks pass. This is a behavior-preserving refactor with no query-reduction claim; no schema, dependency or template changed. Coverage, parallel and browser checks were not rerun. Tests use isolated SQLite and fake external I/O; the application database was not changed.

## Partially loaded guest-table relationships (2026-09-14)

`GuestSessionSecurityHardeningTest` adds six cases for unloaded, table-only and fully loaded `servicePoint.areaNode` graphs, each with and without an assigned area. The two table-only cases initially failed because `areaNode` remained unloaded. Moving the nested relation out of the parent's load callback makes all six pass: exact table/area fields and existing table identity are preserved; preparation costs 2/1/0 queries with an area and 1/0/0 without one; later presentation and repeated calls cost zero.

The final `GuestSessionSecurityHardeningTest`, `GuestSessionFunctionalTest`, `TableSessionTransferTest`, `TableSessionMergeTest`, `GuestJoinApprovalUiTest`, `GuestInviteShareLinkTest` and `SqlitePerformanceGuardrailsTest` run passes 55 tests / 1,067 assertions. Pint, `composer analyse` (665 files) and diff whitespace checks pass. Tests use isolated SQLite; no application database changed. Framework semantics and relationship keys were checked against installed Laravel 13.26.1, official documentation, model definitions and migrations because Boost/schema/Telescope MCP was unavailable. Full-suite, coverage, parallel and browser gates were not rerun for this focused read-service correction; no template, asset, schema or dependency changed.

## Eloquent lazy-loading prevention (2026-09-14)

`EloquentLazyLoadingTest` initially had four failures and three passing cases: local/testing/staging did not enable prevention, and an unloaded collection relationship issued a query instead of throwing. Provider configuration now enables prevention outside production. All seven cases pass, including production selection, rejection before any extra query and a constant two-query eager-loading budget across 2 and 15 users.

The first full Unit/Feature run exposed one additional failure in the demo-role workspace test: waiter inactivity calculation accessed an unloaded session branch. The waiter dashboard now reuses its already loaded, authorized branch models and settings for session presentation. New two-branch fixtures first reproduced this exception and now cover custom/default warning thresholds and different timezones, with exactly 35 queries for both 4 and 40 active sessions.

The final focused run of `EloquentLazyLoadingTest`, `DatabaseSeederTest`, `WaiterDashboardTest`, `SessionInactivityCleanupTest`, `WaiterZoneAssignmentsTest` and `SqlitePerformanceGuardrailsTest` passes 37 tests / 1,116 assertions. The final sequential `php artisan test --compact --testsuite=Unit,Feature` run passes 1,631 tests with 8 skipped / 46,549 assertions in 216.28 seconds. Pint, `composer analyse` (665 files) and diff whitespace checks pass. Tests use isolated SQLite and fake local storage; no application database was changed. Installed Laravel 13.26.1 source, official documentation, migration definitions and test query counts supply evidence because Boost/schema/Telescope MCP was unavailable. Coverage, parallel and browser gates were not rerun; no template, asset, schema or dependency changed in this pass.

## Database seeder model-event invariants (2026-09-14)

`DatabaseSeederTest` first reproduced missing active service-point guard keys for all 24 active QR codes, 13 occupied table sessions and one pending waiter call. The root `WithoutModelEvents` trait suppressed the saving hooks that maintain those keys. Removing it makes the new regression pass: exact active record identities match persisted guard identities after both the initial seed and a repeated seed, without identity churn.

The final `DatabaseSeederTest`, `DemoRestaurantSeederTest`, `DemoOrganizationCrudSeederTest`, `SystemRolesSeederTest`, `BranchCacheInvalidationTest`, `AuditLogTest`, `QrCodeSchemaTest`, `ProjectCleanupConsistencyTest` and `FactoryStatesTest` run passes 77 tests / 1,621 assertions. This includes demo idempotency, QR file stability, all-role HTTP workspace access, production/host restrictions, model uniqueness, observer cache invalidation and audit behavior. Pint, `composer analyse` and diff whitespace checks pass. SQLite `:memory:` and a fake public disk isolate these runs; no application database was seeded or repaired. Framework semantics and unique-index definitions were checked in installed Laravel 13.26.1, official documentation and migrations because Boost/schema/Telescope MCP was unavailable. Full-suite, coverage and browser checks were not rerun; no template, asset, schema or dependency changed.

## Department relationship filtering (2026-09-14)

`KitchenScreenTest` adds seven cases for active, new, accepted, in-progress, ready, completed and cancelled filters. Each fixture has one ticket with mixed item states and another ticket containing only a nonmatching item. Assertions cover exact parent/item identities, total ticket count, the ready-versus-served distinction and a 23-query ceiling. After correcting guarded status assignment in test setup, all seven cases passed before and after the behavior-preserving `withWhereHas` refactor; each instrumented call used exactly 23 queries.

The final `KitchenScreenTest`, `BarDepartmentScreenTest`, `DepartmentAccessActionTest`, `KitchenTicketPrintTest`, `OrderCancellationTest` and `SqlitePerformanceGuardrailsTest` run passes 47 tests / 979 assertions, including existing long-queue, isolation, lifecycle and snapshot checks. Pint, `composer analyse` and diff whitespace checks pass. Tests use SQLite `:memory:`. Boost/schema/Telescope MCP was unavailable; verification used official Laravel documentation, installed Laravel 13.26.1 source, migration definitions and executable test query counts. Full-suite, coverage and browser checks were not rerun for this backend refactor; no template, asset, schema or dependency changed.

## Guest-menu translation subqueries (2026-09-14)

`GuestMenuDisplayTest` adds 15 cases combining EN/LT/RU with translated, missing, empty-name, null-description and whitespace-field data. Other-language rows remain present to detect locale leakage. The tests check parent/item identity, text, integer-cent price, identical warm payloads, zero category/item translation hydration and cold/warm query ceilings of 13/2. Together with the tightened existing query-budget test, all 16 first failed on the prior 15-query/two-hydrated-translation implementation; all pass after scalar projections. The existing invalidation test additionally verifies the updated category description after a translation write.

The final `GuestMenuDisplayTest`, `MenuTranslationManagementTest`, `MenuScheduleTest`, `BranchCacheInvalidationTest` and `SqlitePerformanceGuardrailsTest` run passes 63 tests / 1,400 assertions. Pint, `composer analyse` and diff whitespace checks pass. Tests use SQLite `:memory:`. Boost/schema/Telescope MCP was unavailable; API and index verification used official Laravel documentation, installed Laravel 13.26.1 and migration definitions, with test query counts and model-retrieval events as executable evidence. Full-suite, coverage and browser checks were not rerun for this backend query change; templates, assets, schema, translation catalogues and dependencies are unchanged.

## QR session query cloning (2026-09-14)

The new `GuestSessionSecurityHardeningTest` dataset passes before refactoring: 9 tests / 30 assertions. It covers current and actively merged tables, transferred-session fallback, unrelated same-branch and unlinked tables, a foreign branch despite matching transfer metadata, and closed/cancelled sessions. It checks exact query counts and the returned identity, branch, current table, status and metadata. These are behavior-preservation regressions, not evidence of a pre-existing access-control defect.

After consolidating the shared projection and session/state filters into independently cloned queries, the final `GuestSessionSecurityHardeningTest`, `TableSessionTransferTest`, `TableSessionMergeTest`, `TableSessionLifecycleTest`, `GuestOrderStatusScreenTest`, `TokenSecurityRulesTest` and `GuestSessionFunctionalTest` run passes 61 tests / 604 assertions. Pint, `composer analyse` and diff whitespace checks pass. Tests use SQLite `:memory:`; Boost/schema/Telescope MCP was unavailable, so API verification used official documentation and installed Laravel 13.26.1 source, with migration definitions and test query counts as local evidence. Full-suite, coverage and browser checks were not rerun; no UI, schema, dependency or authorization contract changed.

## Cancellation count aggregates (2026-09-14)

`OrderCancellationTest` adds fixtures with 0, 3 and 501 ready/served items, a pending item on another ticket of the same order, and a ready/served item belonging to a foreign order. All three initially failed: small fixtures hydrated unnecessary item models, and the largest recorded 499 instead of 501 because of the 500-row limit. The regression now checks exact counts in order metadata, status history and audit values; audit organization, branch and actor; foreign-item preservation; zero item-model hydration; and a 19-query ceiling.

The final `OrderCancellationTest`, `OrderSchemaTest`, `OrderStatusLogTest`, `OrderLifecycleStateMachineTest` and `AuditLogTest` run passes 42 tests / 371 assertions. Pint, `composer analyse` and diff whitespace checks pass. Tests use SQLite `:memory:`. Boost/schema/Telescope MCP was unavailable; verification used installed Laravel 13.26.1 source, official relationship documentation, migration index definitions and the test query log. Full-suite, coverage and browser checks were not rerun for this backend change; templates, assets, schema and dependencies are unchanged.

## Conditional menu audit context (2026-09-14)

`AuditLogTest` covers name, description and display-order changes with cast-equivalent price/availability inputs: each persists the requested value, clears guest-menu cache and creates no irrelevant audit. All three initially fail the new 10-query ceiling with 12 queries. An early saved-attribute guard in `MenuItemObserver::recordAuditedChanges` removes the two unused context reads. A combined price/availability change preserves both audit records with exact old/new values, organization, branch and actor; saving the unchanged instance again performs zero queries and does not duplicate them. Existing tests also exercise separate price/availability updates and deletion audit.

The final `AuditLogTest`, `MenuCrudTest`, `BranchCacheInvalidationTest` and `GuestMenuDisplayTest` run passes 61 tests / 644 assertions. Pint, `composer analyse` and diff whitespace checks pass. Change tracking and `updated` event semantics were verified in installed Laravel 13.26.1 source and official documentation; Boost MCP was unavailable, so measurements use the isolated test database's query log. Full-suite, coverage and browser checks were not rerun. No UI, schema, dependency, authorization or audit-content contract changed.

## Owner existence guard (2026-09-14)

`StaffRoleAndInvitationLifecycleTest` verifies that another active owner permits a role change, while inactive owners, non-owner staff and owners in another organization cannot bypass last-active-owner protection. Successful changes retain the other owner's role and audit the previous/new role and reason; denied changes preserve membership and write no role-change audit. Invited, suspended and removed targets keep their status when their role changes, even with no active owner present.

The pre-refactor behavioral baseline passes 6 tests / 30 assertions. All three inactive-target cases then fail the new 9-query ceiling with 10 queries; after the early return and scoped `exists()` replacement, they pass, and the active-target case stays within its 10-query budget. The final `StaffRoleAndInvitationLifecycleTest`, `StaffActionAuthorizationTest`, `StaffManagementUiTest` and `PermissionSystemTest` run passes 43 tests / 565 assertions, including existing self-change, privilege-escalation and cross-tenant denials. Pint, `composer analyse` and diff whitespace checks pass. Laravel 13.26.1 APIs and declared indexes were inspected through installed source, official documentation and migrations because Boost/Telescope MCP was unavailable. Full-suite, coverage and browser checks were not rerun; no UI, schema, dependency or authorization contract changed.

## Guest credential serialization (2026-09-14)

`TokenSecurityRulesTest` adds two model cases and one loaded-relationship case for `TableSessionGuest` and `TableSessionJoinRequest`. All three first failed because `guest_token` appeared in ordinary Eloquent serialization. They now verify credential exclusion from attribute arrays, model arrays, JSON and nested session relationships, while retaining ordinary fields and direct/fresh server-side credential access. Serialization of the already-loaded models performs zero queries.

The final `TokenSecurityRulesTest`, `GuestSessionSecurityHardeningTest`, `GuestCreatedPendingSessionTest`, `GuestJoinApprovalUiTest`, `GuestSessionFunctionalTest` and `GuestInviteShareLinkTest` run passes 51 tests / 481 assertions. Existing coverage verifies cookie entry/restoration, approval, invalid credentials, expiry, revoked access, HTML and CSV token exclusion. Pint, `composer analyse` and diff whitespace checks pass. Laravel 13.26.1's `Hidden` attribute was verified against installed source, existing sibling models and official documentation; Boost MCP was unavailable. Full-suite, coverage and browser checks were not rerun; schema, credentials, authentication logic, UI templates and dependencies were unchanged. The regression establishes a model-serialization gap, not a demonstrated public endpoint leak.

## Menu deletion cascade streaming (2026-09-14)

`MenuSchemaTest` adds four 405-record cases covering menu root categories, remaining categories under an already deleted parent, category children and category items. Reverse display order exposes unsafe ID pagination unless relationship ordering is reset. Tests assert every target deletion event occurs once, all targets are soft-deleted, foreign-menu records survive and at most 200 target models are hydrated before the first deletion.

All four tests first failed with 405 models hydrated upfront and then passed after the observer queries switched to `reorder()->lazyById(200)`. Instrumented query totals increased by exactly two per fixture for the remaining batches; detailed counts and measurement limits are in `performance.md`. The final `MenuSchemaTest`, `MenuCrudTest`, `MenuItemImageGalleryTest`, `LocalMediaStorageTest` and `BranchCacheInvalidationTest` run passes 70 tests / 548 assertions, including existing nested deletion, image cleanup, rollback and cache regressions. Pint, `composer analyse` and diff whitespace checks pass. Framework semantics and existing menu/category/item indexes were inspected using installed Laravel 13.26.1, official documentation and migrations because Boost MCP was unavailable. Full-suite, coverage and browser checks were not rerun; no UI, schema or dependency change was made.

## Dashboard cache locale separation (2026-09-14)

`BasicAnalyticsTest` and `RestaurantDashboardTest` first reproduced English-formatted cached dates being returned after switching to Lithuanian. The new regressions fill EN/LT/RU snapshots, check localized dates and monetary totals, revisit each cached variant, and verify that session/draft mutations invalidate every variant. The restaurant regression also renders the authenticated dashboard over HTTP in all three languages and asserts the displayed date and revenue.

After adding versioned locale-aware keys, the focused `BasicAnalyticsTest`, `RestaurantDashboardTest`, `BranchCacheInvalidationTest` and `LocalizationTest` run passes 34 tests / 871 assertions. Pint, `composer analyse` and diff whitespace checks pass. Query shapes and TTLs are unchanged; each locale gets an independent cache fill. Full-suite, coverage and browser checks were not rerun; no Blade, asset, schema, translation-catalogue or dependency change was needed. Boost MCP was unavailable; the `Cache::flexible` review used installed Laravel 13.26.1 source and official documentation and did not introduce stale-while-revalidate behavior.

## Waiter draft summary loading (2026-09-14)

`BuildWaiterDashboardAction` uses `withSum('items', 'total_price_cents')` alongside `withCount` instead of hydrating draft items only to sum their prices. The sender relation selects only `id` and `guest_name`; the parent retains `sent_by_guest_id`. Integer cents and the empty-draft zero total are preserved in both the branch summary and table preview.

The new `WaiterDashboardTest` regression initially failed for drafts with 3 and 40 lines because all lines were hydrated. Instrumented 0/3/40-line fixtures each measured 39 queries before and 38 after; retrieved `DraftOrderItem` model counts changed from 0/3/40 to 0/0/0. The permanent regression enforces a 38-query ceiling, zero line-model hydration, exact totals, line counts and sender/preview data. The final run of `WaiterDashboardTest`, `WaiterZoneAssignmentsTest`, `WaiterReviewFunctionalTest` and `SqlitePerformanceGuardrailsTest` passes 24 tests / 866 assertions. Pint, `composer analyse` and diff whitespace checks pass. API semantics and existing integer-money/index definitions were inspected in installed Laravel 13.26.1, official documentation and migrations; Boost MCP was unavailable. Full-suite, coverage and browser checks were not rerun, and no page-latency improvement is claimed.

## Staff relationship query refactor (2026-09-14)

`StaffQueryService` and the waiter dashboard assignment lookup use 16 explicit `whereBelongsTo` predicates for existing organization, branch and user models. Relationship names are explicit; additional organization/brand constraints, scalar-ID filters, pagination and eager loads remain intact. The API was verified against installed Laravel 13.26.1 and official documentation; existing model relationships and migration indexes were inspected because Boost MCP was unavailable.

The focused pre-refactor baseline passes 3 tests / 20 assertions. Coverage includes one user assigned to sibling branches and separate organizations, rejecting an out-of-branch assignment ID, and keeping another waiter's zone out of the default dashboard. The final `OrganizationsQueryBudgetTest`, `StaffManagementUiTest`, `WaiterZoneAssignmentsTest` and `WaiterDashboardTest` run passes 41 tests / 388 assertions. Membership lookup with its role remains exactly two queries; staff/invitation lists remain three and zone lookup one in the covered fixtures. Pint, `composer analyse` and diff whitespace checks pass. No schema or interface contract changed; full-suite, coverage and browser checks were not rerun.

## Waiter zone grouping refactor (2026-09-14)

`StaffQueryService::areaAssignments` and `BuildWaiterDashboardAction::assignedAreaNodeIdsByBranch` use `mapToGroups` to build grouped scalar IDs directly. `toBase()` intentionally leaves Eloquent collection semantics before grouping scalar values. Form IDs remain strings; dashboard IDs remain integers with unique, ordered values. Read-only nested ID collections use the existing covariant type convention.

The pre-refactor `WaiterZoneAssignmentsTest` baseline passes 4 tests / 20 assertions. The post-refactor run including `WaiterDashboardTest`, `OrganizationsQueryBudgetTest` and `StaffManagementUiTest` passes 35 tests / 362 assertions. Coverage includes multiple users/zones, stable ID order and type, empty results, branch scoping and the mine/all dashboard filter; staff zone loading remains exactly one query. Pint and `composer analyse` pass. The full suite, coverage and browser checks were not rerun for this collection-only refactor.

## Search query refactor (2026-09-14)

Five same-value OR search groups in `BranchQueryService` and `StaffQueryService` use `whereAny`; `whereAll` would change the required match-any behavior. `OrganizationsQueryBudgetTest` covers each branch/name/contact field independently, nullable invitation contacts, trimmed terms, tenant/branch isolation and pagination. Its pre-refactor baseline passes 12 tests / 221 assertions; staff and invitation scenarios each execute exactly three queries before and after replacement.

The post-refactor run of `OrganizationsQueryBudgetTest`, `StaffManagementUiTest` and `BranchManagementTest` passes 36 tests / 310 assertions. Pint, `composer analyse` and diff whitespace checks pass. The full suite, coverage and browser checks were not rerun for this behavior-preserving query refactor; the monetary-validation full-suite result below predates it.

The service-point follow-up replaces the name/display-number/internal-code OR chain with `whereAny`, retaining the outer group around the active QR relationship alternative. Four field-specific cases cover trimmed case-insensitive search, nullable optional columns, unmatched and inactive tables, sibling-branch and foreign-organization exclusion. Their pre-refactor baseline passes 4 tests / 16 assertions; each fixture executes exactly four queries including eager loading both before and after replacement. The final `OrganizationsQueryBudgetTest` and `ServicePointCrudTest` run passes 30 tests / 348 assertions, with Pint, `composer analyse` and diff whitespace checks passing. No schema or interface contract changed; full-suite, coverage and browser checks were not rerun. Boost MCP was unavailable, so API behavior was checked against installed Laravel 13.26.1 source and official documentation, and schema/index definitions were inspected in migrations.

## Monetary validation regression (2026-09-14)

`InputValidationRulesTest` verifies the reusable `DecimalMoney` rule against incompatible decimal syntax, hostile types, overflow, valid exact values and translated field labels in EN/LT/RU. `MenuCrudTest` reproduces the former `.50` / `1.` conversion exceptions through Livewire, verifies rejected submissions preserve the stored price, and saves `0.29` as exactly 29 cents after correction.

The focused regression reports 36 passed / 77 assertions. The complete Unit/Feature suite (`PAO_DISABLE=1 php vendor/bin/pest --testsuite=Unit,Feature --parallel --processes=4 --compact`) reports 1,553 passed, 8 skipped and 45,650 assertions in 184.90 seconds. Pint and `composer analyse` pass; translation scan reports 652 files, 2,222 used keys per locale and zero missing/unused keys; audit reports 6,666 entries and zero critical issues. Browser, frontend build and full application coverage were not rerun for this server-validation change; templates, assets, schema and dependencies were not changed.

## Current working-tree evidence (2026-08-24)

| Command | Observed result |
|---|---|
| complete table-session/guest/waiter/order/payment regression | 143 passed; 1,109 assertions |
| final lifecycle/traceability/cleanup regression | 35 passed; 1,209 assertions |
| final lifecycle/error/transfer/token regression | 68 passed; 573 assertions; additional QR/transfer/security rerun 44 passed/305 assertions |
| demo lifecycle/seeder/login regression | 63 passed; 1,500 assertions |
| lifecycle/localization/traceability regression | 65 passed; 1,908 assertions |
| `composer ci:check` | Pint clean; Larastan 0 errors; 1,492 tests, 1,484 passed, 8 skipped, 45,489 assertions in 227.56 seconds |
| `composer test:coverage` | 1,487 Unit/Feature tests, 1,479 passed, 8 skipped, 45,077 assertions in 724.66 seconds; 93.5% application coverage |
| `composer test:browser` | 5 passed; 412 assertions |
| `vendor/bin/pint --dirty --format agent` | passed |
| `composer analyse` | passed; 0 errors |
| `php artisan translations:scan` | 650 files; 2,204 semantic keys used and present per locale; 0 missing/unused/phrase keys |
| `php artisan translations:audit` | 6,612 aligned EN/LT/RU entries; 0 critical issues |
| `composer validate --strict` / `composer audit --locked --no-interaction` | valid package; zero advisories |
| `npm run build` | Vite 8.2.2 passed; CSS 303.36 kB / 40.13 kB gzip; JS 4.56 kB / 1.73 kB gzip |
| isolated migration plus repeated default seeds | all 86 migrations passed; seed rerun idempotent; final contraction rolled back and reapplied safely |
| config, route, event and view cache builds | passed and cleared afterward |
| Chrome DevTools | isolated waiter history at 390/768/1,440 px: no overflow or mutation controls on a closed session; clean console; mobile Lighthouse 100 in all four categories |

The later business-invariant pass retains this table's earlier release evidence and adds a newer exact-tree result: `composer ci:check` and the eight-process run each report 1,469 total / 1,461 passed / 8 skipped with 44,588 assertions; coverage reports 1,464 total / 1,456 passed / 8 skipped, 44,176 assertions and 93.6%; Pest Browser reports 5/412. `OrderLifecycleStateMachineTest`, `OrderItemQuantityTest`, `WaiterOpenTableActionTest`, `KitchenScreenTest`, `KitchenTicketPrintTest`, `DraftOrderFunctionalTest` and `WaiterDraftEditingTest` are the focused invariant evidence.

## Complete table-session lifecycle evidence (2026-08-24)

- `TableSessionStatus` is the central access/service envelope. `TransitionTableSessionStatusAction` locks and reloads the database row, rejects stale/backward/terminal transitions and refreshes the caller model. The role datasets prove waiter, director and restaurant administrator open, remove a participant, confirm the first order, close and reopen only inside their assigned tenant; payment management remains separately permissioned.
- First entry, two-process concurrent entry/approval, pending join approval/rejection, leave/removal, no-approver expiry, duplicate request/idempotency, abandoned-session refusal, repeat open, unfinished-close rejection, bill eligibility, temporary-state finalization and a distinct reopen session are covered by Feature and Livewire tests. Closed credentials are cleared without revealing whether an old session existed, while cookie-only and server-session-only recovery independently restore a permitted current identity.
- Transfer restoration is deliberately credential-specific. A known guest, pending request or active invite from the previous permanent QR may resolve the current table only when the session's bounded transfer history proves that origin; ordinary entry and a same-branch QR/token substitution still use the current/active-linked scope and fail closed. Isolated Livewire polling repeats the same authorization.
- The waiter dashboard includes a bounded tenant-scoped closed-session history with eager guest/order counts. Historical detail is read-only on first render, shows retained guest/order records and terminal fulfilment copy, and exposes no forms or Livewire mutations.
- Browser inspection found one real demo inconsistency: open sessions were paired with `service_points.status=free`. `DemoOperationalStateSeeder` now maps every table-session state to its coherent physical status, and the regression checks every open seeded workflow. The local non-production seeder completed safely and a read-only SQLite audit reported zero inconsistent rows.
- A final user-facing review found English fallback labels in the six enums that describe session, participant, join-request, source, waiter-call and physical table states. The regression was first observed failing on `Pending`, then all cases were moved to semantic JSON keys and verified distinct from English in both LT and RU (1 test/124 assertions); the complete lifecycle file passes 19 tests/231 assertions.

## Complete localization audit evidence (2026-08-24)

- `en.json`, `lt.json` and `ru.json` contain the same 2,157 flat semantic dot keys. The latest recorded scanner covers 635 PHP, Blade, JavaScript and MJS files, including direct, indirect and bounded dynamic calls; it reports no missing, extra, unused or phrase-style key. The stricter audit also reports no invalid/nested/empty value, placeholder mismatch, plural-structure mismatch or unapproved English-equivalent fallback.
- Locale precedence and persistence are covered for authenticated profiles, web sessions, active guests and pending join requests. The additive SQLite migration defaults existing guest records to `en`; isolated fresh/seed, rollback and reapply runs succeeded. Locale changes add no read query and write only when the persisted preference actually changes.
- Locale-aware date/time, relative time, integer-minor-unit currency, plural boundaries, validation/domain errors, notifications, status/role labels, accessibility names and first-party Livewire pagination are covered. The complete relational guest menu graph retains required EN/LT/RU translations and `SeededLanguageSwitcherTest` proves names/descriptions change with locale.
- The focused localization batch passes 33 tests with 28,921 assertions. A disposable isolated Chrome context rendered the English landing page, persisted Lithuanian from `/?lang=lt` into a query-free `/login` navigation, and rendered Russian via `/login?lang=ru`; accessibility snapshots were localized and the current console had no warning or error.

## Permanent QR and guest table flow evidence (2026-08-24)

- RED/GREEN regressions cover the waiter-opened first guest only when the session has no previous guest record, abandoned-session rejection, retry-safe credential reuse, the 20-live-request bound, notification deduplication, repeat approve/reject, digest-only rotating guest invites, lock-time invite revalidation, explicit expiry, cross-restaurant rejection, closed-session rescan, dedicated GET/Livewire rate limits, deterministic SVG creation, missing-file repair, rename/move stability, onboarding rollback and explicit reissue cleanup/compensation.
- `GuestSessionConcurrencyTest` creates a separate migrated SQLite file and uses Laravel's process concurrency driver. Two simultaneous entry transactions produce exactly one active opener plus one pending join request; two simultaneous approvals return the same guest ID and leave one accepted membership. The connection uses the deployable `IMMEDIATE`, WAL and busy-timeout settings rather than a mocked lock.
- `DemoRestaurantSeederTest` proves exactly 19 hash-path SVGs and stable repeated-run hashes; plaintext invite bearers are absent from `table_sessions`, serialized models, HTML, logs and exports. A fresh isolated install and an existing-database copy both applied all 84 migrations; the verified forward migration was then applied to the local non-production database without refresh or record deletion.
- Pest Browser passes 5 scenarios/396 assertions. A separate disposable Chrome DevTools context loaded the seeded public QR at 390×844 in dark mode with 200 responses only, no console messages and no overflow. A definition-list grouping defect found by the first audit was corrected; the repeat snapshot passed 28/28 audits and scored 100 for accessibility, best practices, SEO and agentic browsing.

## Invite-only authorization evidence (2026-08-24)

- RED/GREEN regressions cover hash-only credential creation, token-free redirect, recipient binding, new-account registration, existing-account joining, atomic compare-and-set acceptance, replay, expiry, cancellation, reissue rotation, superadmin denial, corrupt tenant chains, generic localized states and both credential/client plus independent client throttling. The rotating-credential test observed RED at request 31 before the independent client budget was added. Mail is faked and asserted empty because no invitation delivery integration is configured.
- Policy and Action tests cover superadmin, owner, director, restaurant administrator, canonical `head_chef` chef and waiter boundaries; equal/higher-role escalation, self changes, cross-organization/branch/brand identifiers, Livewire payload tampering, scoped search, exports and direct Action calls are denied. New corrupt-scope RED cases prove inconsistent branch membership and invitation rows are neither rendered nor mutated.
- The focused invite/staff/tenant/export/query batch passes 137 tests/917 assertions. Final sequential and parallel Pest each pass 1,312 tests/1,303 passed/9 feature-gated with 28,070 assertions. Canonical coverage passes 1,307 Unit/Feature tests/1,298 passed/9 skipped with 27,679 assertions at 93.5%.
- Larastan reports zero errors. Composer validation/platform/audit, npm audit, Vite build, translation audit/scan, config/route/event/view caches and scoped Pint pass. Route-cache inspection confirms four invitation endpoints and no public `register` or `register.store` route. Pest Browser passes 5 scenarios/391 assertions.
- An isolated Chrome context followed an unknown 64-character bearer through 302 to `/invite/pending`; the expected localized 410 page contained no bearer, carried no-store/referrer/noindex controls, had a meaningful accessibility tree, loaded every asset with 200 and had zero horizontal overflow. Chrome's sole error entry is the intentional top-level 410 response, not a JavaScript or asset failure.

## Database integrity audit evidence (2026-08-24)

- TDD RED observed six original failures: missing leading FK indexes, redundant index prefixes, cross-tenant branch/brand acceptance, cross-branch area-parent acceptance, missing guest-opener FK behavior and retained plaintext invitation columns. A seventh RED identified the missing Fortify confirmation datetime cast.
- The focused database/tenant/factory/invitation/schema/query batch passes 175 tests with 1,652 assertions. `DatabaseIntegrityAuditTest` itself passes 10 tests with 24 assertions and automatically audits every table/model FK index, redundant prefix, `BelongsTo` relation, nullable `SET NULL` target, boolean/date cast, soft-delete trait, public uniqueness and hierarchy boundary.
- An isolated SQLite database completed all 80 migrations, `migrate:fresh --seed`, two additional default seed passes, rollback of the four audit migrations, re-migration and `foreign_key_check`/`integrity_check` with no errors.
- The four forward migrations were then applied to the existing local database. A pre-migration copy and post-migration database were compared across 57 non-migration tables using all common columns; there were zero row differences. Tenant consistency checks remained zero and the local database reports no FK/integrity violations.
- SQLite query plans changed from index/full scans to indexed searches for `area_nodes.parent_id`, `service_points.area_node_id`, `audit_logs.user_id` and `(branches.organization_id, branches.brand_id)`. Schema metadata reports no missing FK-leading index and no redundant non-unique index prefix.

The technical-foundation refresh also passed `ProjectCleanupConsistencyTest` and proved all Eloquent reads and persistence are delegated out of Livewire components. The focused onboarding suite currently passes 192 tests with 2,410 assertions. It covers remount from no checkpoint and every successful checkpoint, starter-menu creation without a completion marker, explicit completed revisit, stale/repeated requests and snapshots across every mutation, revoked membership/subscription/branch-assignment snapshots, missing-subscription fail-closed behavior, explicit permission-deny precedence, bare-owner and orphaned staff-role eligibility, direct future-step and capability invocation, malformed navigation and hostile mixed/boolean form payloads, backward editing without duplicate identities, exact maximum boundaries, entity/checkpoint rollback plus retry, full service-point and QR batch rollback plus retry, starter-menu graph rollback, write-once completion including hard-deleted graph reconstruction, the persisted expected table count, malformed type/position rejection before QR/menu writes, hard-deleted area identity reuse, absence of checkpoint-free legacy mutation Actions, disabled and hard-deleted QR recovery, hard-deleted starter-item recovery, real-remount soft-delete hydration, narrow restore and menu-availability policy checks, partial and completed disabled operational references, cross-tenant parent/pivot corruption rejected before foreign relation hydration or writes, stale menu-chain rollback, safe same-branch table replacement, position normalization, locked and absent public-ID tampering, public-QR identity minimization, exact decimal money with binary-float rejection, EN/LT/RU rendering/key parity and a bounded initial/completed mount query budget. An isolated SQLite drill additionally proved the expected-count migration reversible and backfilled three existing ordered links to `3` without changing foreign keys. The focused 178-assertion browser journey verifies EN/LT/RU, keyboard focus, first-invalid-field recovery, combined help/error association, back/edit behavior, refresh/resume, 320–1,440 CSS-pixel reflow and 200% root text scaling without JavaScript errors or horizontal overflow. A fresh isolated Chrome context confirmed the guest redirect from `/onboarding/restaurant`, all login assets at 200, zero 320-pixel horizontal overflow, a complete accessibility tree, zero console messages and Lighthouse accessibility/best-practices/SEO scores of 100.

## Organizations CRUD evidence (2026-08-24)

The complete 26-resource matrix passes 179 focused tests with 1,746 assertions. The dedicated Pest Browser journey creates, edits, archives and restores only its own disposable organization, traverses every nested organization/staff/permission/brand/branch/settings/area/service-point/QR/menu surface, and verifies responsive layout, accessible names, modal focus, 200% text scaling, network success and a clean JavaScript console with 187 assertions. The current full five-scenario browser suite passes with 396 assertions.

An isolated empty SQLite database completed all 76 migrations and two identical demo seed runs. Counts, IDs, deterministic paths and file hashes remained stable, including eight secondary dish-gallery records; a forced production attempt failed closed without changing counts. Only after that proof was the non-production local demo seeder run twice without refresh or truncation. A final isolated Chrome context loaded `/organizations` and the seeded menu/gallery through Herd after the production build: all observed resources succeeded, the account controls had localized accessible names, the viewport did not overflow and the console contained no new error or warning.

## Dish gallery evidence (2026-08-24)

The focused schema, menu Action, CRUD, gallery, local-media and component-size suites passed 72 tests with 524 assertions. They cover multi-file append, the combined eight-image limit, dangerous and oversized files, branch tampering, persistence compensation, primary promotion, individual and parent cleanup, modal closure, edit-only Livewire controls, stable accessible markup and a single eager-load query for every rendered gallery. `ModelFactoryAuditTest` plus `FactoryStatesTest` passed 12 tests with 304 assertions; the gallery migration also completed migrate, rollback and migrate on an isolated SQLite file. Translation audit reported 6,882 aligned EN/LT/RU entries with zero critical issues, the 590-file translation scan reported zero missing/legacy/phrase keys, and Vite 8.2.2 built the production assets successfully. An isolated real browser selected two files in one upload, promoted and removed images, restored the seeded primary, produced zero console errors/warnings after HTTPS URL correction, showed no horizontal overflow at 390 px, and measured LCP 1,297 ms with CLS 0.00.

## Complete menu module evidence (2026-08-24)

The final menu/draft/schema/seeder slice passes 119 tests, 118 passed, one feature-gated/skipped and 1,800 assertions. It covers six translated entity families, owner+locale constraints and cascades, tenant-scoped duplicate rejection, variants, prices, images, modifiers, allergens/dietary labels, stable ordering, indexed temporary hiding, schedule/stock availability, server-trusted localized snapshots and stale unavailable cart rows that remain removable but cannot be changed or sent. The guest payload stays within 15 cold and two warm queries.

After the QR/session changes, the complete sequential and eight-process parallel repository runs each report 1 todo, 8 feature-gated/skipped, 1,408 passed and 29,224 assertions. Canonical coverage passes at 93.6% with 1 todo, 8 feature-gated/skipped, 1,403 passed and 28,828 assertions. The complete browser suite passes 5 scenarios/396 assertions; a separate disposable DevTools context verified the real seeded QR at 390×844 in dark mode, observed a complete accessibility tree, no console warnings/errors, 200 responses only and zero overflow. The final Lighthouse snapshot passes 28/28 audits at 100 in all four reported categories, and the smoke performs no guest/session mutation.

The long CLI gates set `PAO_DISABLE=1` because the optional `laravel/pao` agent-output stream filter raises `stream_filter_remove(): Unable to flush filter` after otherwise completed large runs. This disables only PAO's output formatting; Pest discovery, assertions, failures, skips, parallel execution and the coverage threshold remain unchanged.

The local coverage run used `/Users/andrejprus/Library/Application Support/Herd/bin/php85`, PHP 8.5.8 and Xdebug 3.5.0. `php -m` and `php --ri xdebug` confirmed the extension in that binary, and `XDEBUG_MODE=coverage php --ri xdebug` confirmed coverage mode for the canonical command. The 90% threshold and first-party source set were unchanged. The coverage Composer script disables only Composer's process timeout because the verified run exceeds 300 seconds; Pest still owns failures and its coverage threshold. SQLite backup/restore tests use per-process unique database and local-filesystem roots so simultaneous full and coverage processes cannot corrupt each other's databases, candidates or safety snapshots.

## Canonical order lifecycle evidence (2026-08-24)

- `OrderLifecycleStateMachineTest` proves the centralized forward transition graph, atomic confirm-and-dispatch replay, forbidden order/ticket regressions, actor-bearing ready/served history, closure blocking and cross-tenant confirmation denial. The broader lifecycle selection passes 106 tests, 105 passed, one existing skip and 1,110 assertions.
- `OrderLifecycleConcurrencyTest` uses Laravel's real process concurrency driver against a separately migrated SQLite file using the deployable `IMMEDIATE` transaction mode. Two simultaneous waiter confirmations return one order ID and leave exactly one order, one department ticket, one confirmation log and one dispatch log.
- The same real-process harness sends two simultaneous kitchen accept commands. Both callers converge on `accepted`, the canonical order remains `sent_to_kitchen_bar`, and exactly one ticket-item transition is appended. `KitchenScreenTest` covers every visible production/history state, cancellation, cross-family tampering, stable sorting and a 31-ticket queue whose query count remains 23 while only 24 tickets are hydrated per page.
- The allergen-snapshot migration test creates existing order/ticket rows in a separately migrated SQLite file, rolls the new columns back, proves both records survive, reapplies the migration and verifies safe empty defaults. `KitchenDelayTimerTest` proves active timers advance while completed timers freeze; disposable Playwright checks at 390, 1,024 and 1,440 CSS pixels found no horizontal overflow after the tablet breakpoint correction, 64px working actions, named controls, exact bar department options and a 403 for bartender access to kitchen.
- Waiter editing/ownership, kitchen/bar isolation, ready/service, cancellation, bill request, manual payment, repeat orders and the vertical guest-to-closed-table scenario remain in the focused selection. Policy/tenant/architecture regressions add 123 passing tests and 760 assertions; translation audit reports zero critical issues and Larastan reports zero errors.
- Final repository verification passes sequentially and on eight processes with 1,414 passing tests, one todo, eight feature-gated skips and 29,361 assertions. Coverage is 93.5% over the Unit/Feature set; the five-scenario browser suite passes with 410 assertions. The paid-table browser journey now crosses waiter confirmation, kitchen processing, service, bill request, manual payment and table close. A separate disposable Chrome DevTools context loaded the Boost-resolved login URL with a complete accessibility tree, only 200 responses and an empty warning/error console.

## Preserved browser evidence

Before the merge, Chrome DevTools used distinct disposable isolated contexts for the restaurant UI and every demo role. The checked flows covered all 12 one-click role logins, authenticated switch prevention, missing/restored account behavior, invitation registration boundaries, EN/LT/RU, keyboard submission and focus, light/dark rendering, and 320, 360, 768 and 1,440 CSS-pixel layouts without horizontal overflow. Chrome and Playwright reported no console warnings/errors or failed assets; demo Lighthouse accessibility scored 100 on mobile and desktop.

On 2026-08-23, an additional disposable Chrome pass covered the waiter dashboard and table detail at 1,280 and 390 CSS pixels. The checked pages had no horizontal overflow, no checked interactive target below 24 CSS pixels, visible focus through the keyboard order, no console warnings/errors, and only successful network responses after applying the pending forward migrations to the local demo database. The complete four-test Pest Browser suite also passed in Playwright WebKit 26.5 with 113 assertions. Playwright Firefox 153 could not start on macOS 27 because of the [confirmed upstream sandbox failure](https://github.com/microsoft/playwright/issues/42082); a minimal browser launch reproduced the runtime failure before application navigation. Physical assistive technology/device, actual Safari/Firefox, and non-headless 200% zoom remain environmental limitations.

## Automated architecture boundaries

`ProjectCleanupConsistencyTest` and related repository tests prohibit route SFC/Volt, Blade PHP blocks, Blade model/Action/Service/Illuminate/facade/container/auth/config/session access, application service locators, hardcoded Livewire page titles/action errors, debug stubs, forbidden runtime packages and legacy translation keys. XSS, token, route, tenant, file, money, cache and query tests enforce the corresponding requirement families. Invitation tests also prove bearer tokens never appear in rendered Livewire snapshots.

## Factory-first demo portfolio evidence (2026-08-24)

The focused factory, seeder and demo-login selection passes 75 tests with 1,804 assertions. It covers every factory status dispatcher, three isolated tenants, all 12 canonical demo roles, all draft/order/invitation status cases, preserved password hashes, production and host denial, legacy invitation-digest rotation, repeated-run IDs/hashes/file paths and successful access to each prepared role workspace. The dedicated organization Chromium journey passes 187 assertions and the full five-scenario browser suite passes 410 assertions.

Final-code isolated SQLite verification completed all 86 migrations with `migrate:fresh --seed`, repeated the default seed and retained exactly 24 QR records/files. Sequential and eight-process parallel Pest each pass 1,458 total/1,450 passed/8 skipped with 44,532 assertions. The canonical Unit/Feature coverage run passes 1,453 total/1,445 passed/8 skipped with 44,120 assertions at 93.5%; its Clover diagnostic executes all 208 Actions and covers 94.8% of their statements. Composer validation/audit, npm audit, Pint, Larastan, production build, translation audit/scan and cache compilation pass.

## Concurrency and idempotency audit (2026-08-24)

`CriticalOperationConcurrencyTest` uses Laravel's real process driver against a separately migrated file SQLite database configured with production-equivalent `IMMEDIATE` transactions, WAL and busy timeout. Paired processes exercise existing-user invitation acceptance, new-user invitation registration, guest draft add/update/delete, QR generation/reissue, canonical order status progression and table close. It passes 1 test/35 assertions and proves one membership/audit, one command-owned draft row/history transition, one active/replacement QR, one order status log and one terminal close audit. The existing `GuestSessionConcurrencyTest` and `OrderLifecycleConcurrencyTest` retain real-process first-entry/approval and waiter-confirm/dispatch coverage.

Repeated Livewire requests now cover guest and waiter update/delete, send-to-waiter, approve/reject join, waiter confirmation and table close. Action regressions also cover same-input retries after a simulated lost response, reject ambiguous invitation registration or foreign-owned stale item deletion, and prove that a non-active guest cannot expire a join request before authorization. The affected concurrency/action/Livewire slice passes 113 tests/1,054 assertions; the final waiter-confirm/table-close replay slice passes 12/145. `DatabaseIntegrityAuditTest` asserts the unique persistence identities used by these workflows.

The final exact-tree `composer ci:check` run passes Pint, Larastan with 0 errors, and 1,502 total tests/1,494 passed/8 feature-gated skips/45,581 assertions in 232.21 seconds. Fresh Unit/Feature coverage passes 1,497 total/1,489 passed/8 skips/45,169 assertions at 93.5% in 730.36 seconds. Vite 8.2.2 production build passes with 303.36 kB CSS (40.13 kB gzip) and 4.56 kB application JavaScript (1.73 kB gzip).
