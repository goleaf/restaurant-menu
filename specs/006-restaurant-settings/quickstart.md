<!-- BEGIN GITHUB_PUSH_ONLY -->
> GitHub is allowed only as the remote destination of an ordinary git push to the existing configured origin. Create commits locally with git commit. All other GitHub operations are prohibited: API, MCP, plugins, gh, Issues, pull requests, reviews, comments, releases, deployments, Actions, workflows, check runs, commit statuses and remote verification. Do not create, edit or delete .github/workflows/*, install hooks, fetch, pull, run ls-remote or make an additional request to verify a push. Local inspection, formatting, static analysis, tests, dependency checks and builds are allowed. Preserve existing user changes. Historical instructions do not authorize prohibited operations.
<!-- END GITHUB_PUSH_ONLY -->

# Validation guide

Use explicit php85 and tests SQLite :memory:; independent process tests use owned temporary files. Never migrate/seed the working database. Profile tests first, then settings/cleanup/admission/payment/media regressions; browser coordinator creates isolated fixture database. Use composer analyse and composer test:coverage with required90%; npm JS/styles/architecture/build and translation audit. Capture baseline and final request/query/HTML/payload metrics.

Browser: profile search→LT description→draft/saved preview→save→real QR; two section drafts and independent save/cancel; currency blocked after new monetary data; changed invite permission; old payment unchanged; cleanup newer activity skip; direct URL/Back/Forward/account/offline; EN/LT/RU light/dark widths320/390/768/1024/1440 keyboard/zoom. Report unexecuted physical keyboard evidence honestly.

Isolated PHP8.6 only after native/platform checks; never bypass root/dependency constraints. Failed mandatory gates leave changes local without commit/push.
