---
paths:
  - resources/views/components/dashboard/branch-picker.blade.php
---

<!-- BEGIN GITHUB_PUSH_ONLY -->
> GitHub is allowed only as the remote destination of an ordinary git push to the existing configured origin. Create commits locally with git commit. All other GitHub operations are prohibited: API, MCP, plugins, gh, Issues, pull requests, reviews, comments, releases, deployments, Actions, workflows, check runs, commit statuses and remote verification. Do not create, edit or delete .github/workflows/*, install hooks, fetch, pull, run ls-remote or make an additional request to verify a push. Local inspection, formatting, static analysis, tests, dependency checks and builds are allowed. Preserve existing user changes. Historical instructions do not authorize prohibited operations.
<!-- END GITHUB_PUSH_ONLY -->

# Dashboard

## Keep branch selection local, bounded and announced
Keep the native details host for offline close and focus restoration; compose installed Free Flux input/radio controls and retain the selected permitted branch outside search results. Flux 2.17 custom radio groups do not mirror disabled to aria-disabled: the small component-owned MutationObserver does, and disconnects on destroy. Keep explicit help/error IDs and aria-describedby; card rows need flex-none to prevent clipping inside a bounded vertical scroller. BranchControlWorkflowTest covers offline blocking, reconnect, focus and scroll geometry. Reevaluate this bridge on Flux upgrade.
