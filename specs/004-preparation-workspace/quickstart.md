<!-- BEGIN GITHUB_PUSH_ONLY -->
> GitHub is allowed only as the remote destination of an ordinary git push to the existing configured origin. Create commits locally with git commit. All other GitHub operations are prohibited: API, MCP, plugins, gh, Issues, pull requests, reviews, comments, releases, deployments, Actions, workflows, check runs, commit statuses and remote verification. Do not create, edit or delete .github/workflows/*, install hooks, fetch, pull, run ls-remote or make an additional request to verify a push. Local inspection, formatting, static analysis, tests, dependency checks and builds are allowed. Preserve existing user changes. Historical instructions do not authorize prohibited operations.
<!-- END GITHUB_PUSH_ONLY -->

# Validation guide

Use explicit supported PHP and isolated SQLite fixtures. Do not seed/migrate the working database. Run focused preparation/access/aggregate/transition/print tests, then canonical full backend/coverage and browser coordinators in an owned source snapshot. Preserve90% PHP and100% JS line gates. Run translations:scan/audit, Pint, composer analyse, architecture/JS/styles/build and dependency validation. Record exact source/runtime and discovered/executed inventories.

Browser: confirm mixed draft as waiter; cook sees kitchen only, bartender bar only, authorized overview both; start one, ready one, waiter serves it while other work stays active. Inspect partial ready filter, exact ticket links, snapshot variant, print, second-tab stale command, failed delivery retry, offline/reconnect, revoked access and compact mode. Capture real screenshots320/390/768/1024/1440 in EN/LT/RU, light/dark, keyboard/focus, CSS200%, forced-colors/reduced-motion. Browser fixtures own DB/session/storage.

Check actual isolated PHP8.6 binary and real Composer platform requirements separately. A blocked8.6 application gate is not syntax/application acceptance and does not permit vendor bypass. Commit/push follow review and required stable gates; ordinary push result is the final remote evidence.
