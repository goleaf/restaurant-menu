---
paths:
  - 'packages/livewire/flux-pro/**'
---

<!-- BEGIN GITHUB_PUSH_ONLY -->
> GitHub is allowed only as the remote destination of an ordinary git push to the existing configured origin. Create commits locally with git commit. All other GitHub operations are prohibited: API, MCP, plugins, gh, Issues, pull requests, reviews, comments, releases, deployments, Actions, workflows, check runs, commit statuses and remote verification. Do not create, edit or delete .github/workflows/*, install hooks, fetch, pull, run ls-remote or make an additional request to verify a push. Local inspection, formatting, static analysis, tests, dependency checks and builds are allowed. Preserve existing user changes. Historical instructions do not authorize prohibited operations.
<!-- END GITHUB_PUSH_ONLY -->

# Flux Pro

## Preserve the complete Flux Pro distribution behind a verified Composer boundary
Keep package PHP, templates, compiled assets and proprietary license together; record complete hashes and truthful provenance in the adjacent manifest files. Source presence is not runtime installation: resolve the real Free/Pro constraints and use normal Composer discovery with a physical vendor mirror before enabling components. Preserve the original root flux-pro directory until clean-release and rollback gates prove independence. Root Pint uses anchored notPath patterns verified against nested first-party names; do not format distribution files through Pint stdin/editor mode, whose filter semantics differ. First-party adapters remain in app/resources and retain their normal checks.

## Use the user-selected local adaptation without upstream version claims
The user selected only existing local Flux Pro code on 2026-09-16, superseding the earlier wait for official package access. Maintain explicit local release numbering, keep upstream_version unknown, retain the original inventory and record every compatibility patch. Dependency constraints may be changed only for the locally tested compatibility target; preserve all licensing/edition behavior and normal Composer discovery. No new archive or account access is a prerequisite for this local adaptation.
