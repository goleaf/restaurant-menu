---
paths:
  - 'resources/js/alpine/components/navigation-search.js'
  - 'app/Livewire/Organizations/Brands/Branches/Menu/Dish.php'
---

<!-- BEGIN GITHUB_PUSH_ONLY -->
> GitHub is allowed only as the remote destination of an ordinary git push to the existing configured origin. Create commits locally with git commit. All other GitHub operations are prohibited: API, MCP, plugins, gh, Issues, pull requests, reviews, comments, releases, deployments, Actions, workflows, check runs, commit statuses and remote verification. Do not create, edit or delete .github/workflows/*, install hooks, fetch, pull, run ls-remote or make an additional request to verify a push. Local inspection, formatting, static analysis, tests, dependency checks and builds are allowed. Preserve existing user changes. Historical instructions do not authorize prohibited operations.
<!-- END GITHUB_PUSH_ONLY -->

# Branches Menu

## Keep pending page departures guarded; narrowly allow audited dish history sync
Keep every tracked request in the global pending barrier: ordinary Livewire navigation and native departure remain blocked. Never exempt all $set messages, because update hooks may persist data. Only known history traversal within the same Dish URL, differing solely in section/language, may pass while every pending request is the audited pure-$set UI sync (section/contentLanguage plus optional deferred editingItemForm and read-only returnFilters). Unknown fields, components, URLs, mixed actions and concurrent writes remain guarded. Re-review this contract if those hooks change; retain both the delayed Forward and pending-departure regressions and the documented older-browser unknown-history limitation.
